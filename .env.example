# Local development only. On Railway add these under Service -> Variables.
GOOGLE_CLIENT_ID=

# Optional local override. Railway Volume mounted at /data is detected automatically.
# DATABASE_PATH=/data/app.db
